<?xml version="1.0" encoding="UTF-8"?>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://docbook.org/ns/docbook ../../docbook-5.0/xsd/docbook.xsd " xml:id="filtertypes_before">

	<title>Before</title>

	<para>
		This filter dispatches a new message to a defined target and selector. After the dispatch of the new message the
		filter processing will continue with the old message.
	</para>

	<section>
		<title>Behavior</title>

		<variablelist>
			<varlistentry>
				<term>Filter activity</term>
				<listitem>
					<simpara>Accept-call</simpara>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term>Message flow</term>
				<listitem>
					<simpara>Continue</simpara>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term>Message substitution</term>
				<listitem>
					<simpara>No. The target and selector value of the message are used as filter arguments.</simpara>
				</listitem>
			</varlistentry>
			<varlistentry>
				<term>Resource operations</term>
				<listitem>
					<simpara>...</simpara>
				</listitem>
			</varlistentry>
		</variablelist>
	</section>

	<section xml:id="filtertypes_before_arguments">
		<title>Arguments</title>

		<informaltable border="1">
			<tr>
				<th>Name</th>
				<th>Type</th>
				<th>Required</th>
				<th>Description</th>
			</tr>
			<tr>
				<td>target</td>
				<td>type</td>
				<td rowspan="2">at least one must be set</td>
				<td rowspan="2">
					This defines the message that should be dispatched. Alternatively to setting the
					<code>filter.target</code>
					and/or
					<code>filter.selector</code>
					, the target and selector of the message can be set. The method identified by this new message must
					have a signature that either takes a no arguments at all, or a single argument that is compatible
					with the join point context that is passed to it.
				</td>
			</tr>
			<tr>
				<td>selector</td>
				<td>method</td>
			</tr>
		</informaltable>
	</section>

	<section>
		<title>Examples</title>

		<para>...</para>
	</section>

</section>
